rem Before running this batch file, install node from http://nodejs.org/ (make sure you also install npm, the node package manager - it should
rem come bundled with the installer). Due to a bug in npm for windows, npm may error when running this batch file. You will need to create 
rem a folder called "npm" at the location specified in the error message.

npm install less csso